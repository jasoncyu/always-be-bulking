import {
  selectAddWorkoutPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectAddWorkoutPage();

describe('selectAddWorkoutPage', () => {

});
